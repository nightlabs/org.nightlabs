<project name="IpanemaBoot" default="build" basedir=".">
	<target name="build" 
        description="Builds " >
		<property name="distDir" value="dist"/>

<!--
		<taskdef name="setEclipseVar"
		                 classname="org.nightlabs.ant.EclipseVar"
		                 classpath="../org.nightlabs.ant/runtime/"/>
		                 	
		<setEclipseVar varName="LOG4J"/>
-->

    	<!-- Create the distribution directory -->	    
<!--		<delete dir="${distDir}"/> -->
		<mkdir dir="${distDir}"/>

		<!-- Create DelegatingClassLoader jar -->	    
	    <jar jarfile="${distDir}/DelegatingClassLoader.jar" basedir="bin">
	    </jar>

	    <!-- Copy libraries to libDir -->	
<!--        <copy file="${LOG4J}" tofile="${distDir}/log4j.jar" />
-->
		<copy file="${distDir}/DelegatingClassLoader.jar" todir="../org.nightlabs.classsharing/lib" />
  	</target>    
</project>