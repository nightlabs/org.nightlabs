<project>
	<target name="deploy.pre">
		<!--
		The project has been renamed and we need to prevent collisions in the server.
		TODO This delete task can be removed in a few months (end 2007).
		-->
	    <delete 
	    	dir="${j2ee.deploy.dir}/$${j2ee.deploy.subdir}" 
	    	verbose="true"
	    	failonerror="false"
	    	/>
	</target>
</project>